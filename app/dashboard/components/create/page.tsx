import Form from "@/components/components/form-create";

const ComponentsPage = () => {
  return (
    <div className="h-full flex items-center justify-center">
      <Form />
    </div>
  );
};

export default ComponentsPage;
